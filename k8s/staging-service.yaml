apiVersion: v1
kind: Service
metadata:
  name: hayy-api-staging-service
  namespace: hayy-staging
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: hayy-api-staging